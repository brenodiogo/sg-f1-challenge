spring:
  datasource:
    url: jdbc:tc:postgresql:16:///f1betting
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

openf1:
  base-url: http://localhost:9999
